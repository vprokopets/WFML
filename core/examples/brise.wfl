abstract Region {
	sa: SamplingStrategy
}

abstract SamplingStrategy {
    RegionID -> string
    test -> integer
    xor Strategy {
        Sobol
        MersenneTwister
    }
    [test > 2]
   [RegionID in Context.ExperimentRegions.RegionList]
}

Context {
    ExperimentRegions {
        RegionList -> array
		[fcard.Regions = size RegionList]
		Regions: Region
    }
}
